const st = (theme) => ({
    body: {
        background: theme.palette.dialog.backgroundColor,
    },
    footer: {
        background: theme.palette.dialog.backgroundColor,
        borderTop: '1px solid ' + theme.palette.divider,
    },
});
export default st;
