const st = (theme) => ({
    content: {
        background: theme.palette.dialog.backgroundColor,
    },
    ex: {
        padding: '8px 8px',
        background: theme.palette.input.backgroundColor,
        marginTop: '8px !important',
    },
});
export default st;
